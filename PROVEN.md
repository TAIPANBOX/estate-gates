# What has actually been run, when, and by what

Companion to `OUTSIDE-VIEW.md`. That file is regenerated and answers "what does
a stranger find". This one cannot be regenerated, because it records **runs**,
and answers the question that keeps being answered from memory and answered
wrong: **has this ever actually been executed, on what, and when.**

## The rule this file exists to enforce

A line in a handoff that says "not proven" is scoped to the change it was
written beside. It is not a statement about the component. On 2026-09-01 a
scoped line about one environment variable was read as "this launcher has never
been brought up", and that wrong claim was carried into a report and into two
answers before it was checked. The same session also claimed Linux was never
tested, with three clouds' evidence sitting on disk.

So every row below carries **three** things and is refused without them:

1. **When**, as a date, never "recently".
2. **On what**, as a machine or a cluster, never "in CI" unless CI is the subject.
3. **The artifact**, as a path or a command somebody else can open.

And every row states its **scope**, because a run proves what it ran and
nothing adjacent. "GCP passes" measured on one cluster is not "GCP works".

## Refreshing this file

For the outside view, run the script; do not edit by hand:

```
./scripts/outside-view.py --write OUTSIDE-VIEW.md
```

For this file, add a row when a run happens. Never promote a row from reasoning:
a claim becomes `proven` only after a run, and only the run's own output moves it.

---

## Deployment, proven

| What | When | On what | Artifact | Scope |
|---|---|---|---|---|
| Five-node k3s cluster, all planes answering | 2026-07-25 | Hetzner, 5 x CPX42, Ubuntu 26.04, `fsn1` | `stack-k8s/evidence/cluster-verified.md`, `loadbalancer-verified.md`, `freeze-test-verified.md`; `PORTABILITY.md` §1 | about 25 min bring-up; `security-tests` 23 passed, 1 noted |
| The same stack on AWS | 2026-07-25 | self-managed k3s on EC2, 5 nodes | `stack-k8s/cloud/aws/evidence/aws-run-verified.md` | about 24 min; `cluster-verified` 10/0; `security-tests` 22 passed, 0 failed, 2 noted |
| The same stack on GCP | 2026-07-26 onward | 5 x `c2d-highcpu-8`, `europe-west3` | `PORTABILITY.md` §3 | 28 min 44 s at five nodes; `cluster-verified` 10/0; `security-tests` 24 passed, 0 failed, 2 noted. The load balancer never passed traffic: GOTCHAS 69, open, and marked Platform |
| `stack-single` installed and running on a clean machine | 2026-08-02 | one AWS `c7i.2xlarge`, since destroyed | `Execution journal/live-runs-2026-08-02.md` items 1 to 4 | Four defects found and fixed (stack-single#9, #10, heraldyx#4, `.env` quoting). Proof it ran: heraldyx delivered a live alert from the box |
| Node builds nothing; every pinned image pulled | 2026-09-01 | GCP cluster, five that day plus one AWS | `Execution journal/deployments.md` §3.1 | Deploy 17:16 to 13:32 to 11:33. `verify.sh` 12/0, `security-tests.sh` 27 passed / 0 failed / 3 noted. USD 4.78 for the day, both clouds verified empty afterwards |
| `stack-single`, three installs of the same box, same day, same machine type | 2026-09-01 | three GCP `e2-standard-2`, 2 vCPU, Ubuntu 26.04, each a fresh box, all destroyed | `go-to-market-2026-09/evidence/stack-single-three-installs-2026-09-01.md`, which carries the stamps, the exit codes, the check counts and the pull list. The full install logs are NOT there: they lived on the three boxes and were not copied off before the teardown, and the evidence file says so rather than implying otherwise | **Compiling everything (pre-#27): 51 minutes and it had not finished**, 9 of 10 images built, still on the console. **Planes pulled, door built (#27): 442 s**, exit 0, ten containers, 18 checks `ok`. **Nothing compiled at all (#28): 111 s**, exit 0, nine containers plus `init-volumes` exited 0, 19 checks `ok`, 0 failures, console 200, every image from ghcr.io. The first was stopped rather than finished, so its figure is a floor and not a total |
| `stack-up` from a cold cache on macOS | 2026-09-01 | this Mac, Apple Silicon, empty `CARGO_HOME` / `GOMODCACHE` / `GOCACHE` / npm cache, fresh `STACK_UP_HOME` and `TAIPAN_HOME` | `go-to-market-2026-09/evidence/stack-up-cold-run-2026-09-01.log` | 399 downloads, 353 compiles, zero reuse. **221 s** to a working dashboard; 10 records sealed, packed, verified offline, `VERIFIED`, exit 0. Run with `--no-tools`, so four installed-not-started tools sat outside it |
| The 2026-08-31 `WARDRYX_DSN` to `WARDRYX_DB` change (stack-single#26), through a full `stack-single` bring-up | 2026-09-01 | three GCP `e2-standard-2`, destroyed (the same three installs above) | `go-to-market-2026-09/evidence/stack-single-three-installs-2026-09-01.md`, plus `install.sh:691-702` for the check names | Both installs on top of #26 (stack-single#27 and #28) ran `install.sh`'s own section 8, which is where `WARDRYX_DB` is read: `wardryx answers inside`, `policy store is up`, `policy plane accepts its admin key` (200), `policy plane rejects an unknown key` (401), `gateway's key cannot write policy` (403), `wardryx is NOT on the host`, all `ok` in both runs (18 and 19 checks respectively, 0 failures). Exercised through `install.sh` and `docker compose up`, by the installer's own checks, not by a separate test |
| `stack-single` v1.1.3 on forge, the appliance shape: Debian 13 with Docker CE pre-installed, `GATEWAY_BIND` on the box's tailscale address, `WITH_RECORD` | 2026-09-17 | forge (GMKtec N150, Debian 13, 192.168.1.10 / 100.79.245.43), still running | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `boxes.log`, `ins-phase-a.log`, `logs/install-1.log` | Phase A 61 s, exit 1: 22 `ok` and check 1 FAIL by construction (it probes loopback whatever `GATEWAY_BIND` says); `curl 100.79.245.43:4100/healthz` 200, the LAN address refused. Before it, `apt-get install -s docker-buildx` on Debian 13 showed it would REMOVE docker-ce; held with `apt-mark hold` and a `Pin-Priority: -1` on Debian's `docker-buildx` |
| The same launcher's second run with the appliance override: costcrew v0.2.0 beside the stack, a second gateway for the crew, client keys and two identity maps on the customer door, heraldyx to a file, record-seal every 120 s | 2026-09-17 | forge | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `ins-phase-b.log`, `logs/install-2.log`, `override-config.log`, `compose.override.yaml`, `identity-*.json` | 36 s, exit 1, the same 22 `ok` and one FAIL. The first attempt aborted before the checks: heraldyx's `subpath` mount raced `init-costcrew` (fixed with `depends_on`); the v0.2.0 console has no `-gateway` flag (only `costcrew-run` has). The `:?` guard refuses `compose config` with the client keys blank |
| Two customer agents on two clouds at once, k3s Jobs, through the box's gateway over the tailnet to the real Anthropic API, each refused on its own budget | 2026-09-17 | GCP `e2-medium` europe-west3-a and AWS `t3.medium` eu-central-1a, single-node k3s, ephemeral tailnet nodes, both destroyed 12:45Z | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `pod-probe-*.log`, `agents-run1.log`, `runs-and-bus-after-agents.log` | Direct WireGuard paths (23 and 29 ms); a pod reaches the gateway. 11:46:22Z to 11:47:15Z both: 17 x `200` at 0.000033 with `wardryx=allow`, `402 budget_exceeded` at calls 18 to 20; `/v1/runs` 0.000561 of 0.0006 each; six `breaker_tripped` interleaved by `ts` with a `prev_hash` chain |
| The box freezes one cloud's agents through the policy plane while the other cloud keeps running; identity is bound to the key | 2026-09-17 | same VMs | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `freeze-gcp.log`, `identity-checks.log` | `PUT /v1/policies/freeze-gcp` (deny_above_usd 0.000001): GCP `403 wardryx=deny` from call 1, AWS 17 x `200` in the same minute, three `policy_deny` (high), `DELETE` then GCP `200` again. No key `401`; the AWS key with a GCP id `403 identity_mismatch` (high, on the bus); a wrong key `401` |
| The FinOps crew on the box through its own door, up to a monthly unit cap set centrally | 2026-09-17 | forge | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `crew-run.log`, `units-cap.log`, `crew-spend-discrepancy.log` | `costcrew-run -live -only 294` under a 0.10 cap: one real sonnet-5 call the gateway settled at 0.05811, two `tool refused ... budgets-read`, then `402 unit 'finops' monthly budget exceeded`; under 0.004 task 300 refused before any call; `unit_cap_exceeded` (high) and `breaker_tripped` on `tokenfuse-crew.ndjson`. The runner printed `Spent 0.0116` (its own worst case) against the gateway's 0.05811: costcrew finding |
| The notifier, the console API, idryx and the record over all of the above | 2026-09-17 | forge | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `heraldyx-mail-and-journal.log`, `console-api.log`, `record-cloud-mail.log` | heraldyx 17 mails, chain verifies, an owner line from the passports; console `money_runs`, `money_incidents` (4 `budget_exhausted`), `identity_list_alerts` (`runaway_agent`), `bus LIVE`; record-seal 34 records in 7 segments, packs `VERIFIED` with the tool's own `[weak]` notes; `/v1/audit/verify` ok |
| The failure matrix on the appliance, 16 cases | 2026-09-17 | forge and the two VMs | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `failures/*.log` | PASS as designed: wardryx down fails CLOSED (`403 wardryx unreachable`, `dependency_failed` policy_plane); provider unreachable `502` + `dependency_failed` stage send; retired model `404` + `dependency_failed` stage response; revoked key `401` silent by design; loop signature `402 loop_detected`; alert storm one `suppressed:8`; Cloud/notifier/sealer/console down and up (sealer catches up, 52 records VERIFIED); `down`/`up` keeps everything but the gateway's in-memory ledger; tampered pack `NOT VERIFIED`; policy-db down keeps decisions from memory. FOUND: a dead customer node and a broken path are invisible to every plane; reboot 1 left the gateway `Exited (128)` (docker bound the tailscale address before tailscaled had it), fixed with `net.ipv4.ip_nonlocal_bind=1` plus a docker.service `After=tailscaled` drop-in and PROVEN by reboot 2 (gateway back on its own in 40 s); wardryx `healthz 200` with Postgres down and a policy write that hangs; heraldyx silent after the ceiling; 40 identical plain calls are not a loop; a 40-entry delegation chain accepted silently (delegation off by default); the Cloud's outage-time telemetry lost; clock skew on a VM had no effect in 3 min |
| The FinOps block on the appliance | 2026-09-17 | forge | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `finops-1-3-outcomes-focus-reports.log`, `finops-4-6-cloud-console-unitcap.log`, `finops-7-crew-imports-focus.log` | `x-fuse-outcome` tags priced (`tokenfuse outcomes`), FOCUS export 272 rows customer door and 5 rows crew door, `savings` 0.002498 across 17 breaks, `sql` over `calls` by unit and decision, `compliance --markdown`; the Cloud's `/v1/spend|owners|summary|savings|series|units|incidents|audit/verify`; console `money_overview`, `stats_counts {scan,window_days}`; a central unit override to `402 unit_budget_exceeded` on the customer door (after the restart-reset ledger was accounted for); the crew's FOCUS connector reads the export (277 rows, 4 agents, $0.07) but the import with `replace-generated` is refused by a costcrew v0.2.0 defect (`generated_estate_replaced` carries an empty `severity`); the Cloud's own export file must be pre-created (`10001:10001`) or its incidents never reach the bus |
| Teardown, both accounts verified empty | 2026-09-17 | GCP `stack-k8s-gcp`, AWS `stack-k8s` profile eu-central-1 | `go-to-market-2026-09/evidence/forge-appliance-2026-09-17/` `teardown.log`, `tailnet-after.log`, `boxes.log` (launch and termination stamps) | GCP instance deleted 12:45:01Z, AWS terminated 12:46:26Z, the security group deleted; instances, disks, addresses, volumes all empty by direct query; about 63 VM-minutes per cloud, about USD 0.11 of VM time and USD 0.066 of tokens for the day. The two ephemeral tailnet nodes showed `offline` a minute later |
| `stack-single` main `c18d214` upgraded IN PLACE over the running 2026-09-17 appliance (tokenfuse v1.0.1 to v1.0.3, heraldyx v0.2.3 to v0.2.4, costcrew v0.2.0 to v0.2.2) | 2026-09-23 | forge | `go-to-market-2026-09/evidence/forge-appliance-2026-09-23/` `logs/upgrade-2026-09-23.log`, `logs/snap-before.txt`, `logs/snap-after-upgrade.txt` | 54 s, 25 checks ok, 0 FAIL, the gateway published on the tailnet address only and loopback refused; `.env` identical, the Cloud's month, 246 event lines and the record kept. The first upgrade of a live launcher over two releases. |
| The 2026-09-17 findings re-measured on the released set: tokenfuse #292 to #296, heraldyx #71 | 2026-09-23 | forge, agents on the Mac over the tailnet | `go-to-market-2026-09/evidence/forge-appliance-2026-09-23/` `SUMMARY.md` table, `logs/agent-*.log`, `logs/heraldyx-mail.txt` | all confirmed: budget_exhausted in the Cloud's export file; `seeded aws=39003` after a restart and the unit cap at call 7; 5 calls made while the Cloud was down all arrived; owner `platform-lead` not `unassigned`; `run_stalled` raised; the notifier held at 20/h, summarised at 10 min, let a critical through in the same second. Two agents on real haiku: 17 x 200, 402 on 18. |
| wardryx policy store down, from a `main-8d6bdc3` image BUILT ON THE BOX (#63, not in a release) | 2026-09-23 | forge | `go-to-market-2026-09/evidence/forge-appliance-2026-09-23/` `logs/F4-main.log`, `f4.sh` | readyz 503 `store: unreachable` with healthz 200; PUT answers 503 in 3.003 s, the change not in force; store back in 1 s; one log line each way. Not a release measurement. |
| Load on the appliance's customer door, mock upstream (money invented, latency and refusals real) | 2026-09-23 | forge, the client on the same box | `go-to-market-2026-09/evidence/forge-appliance-2026-09-23/` `logs/load-stub.log`, `loadgen.go` | 1/10/50/100 agents: 22/202/160/118 calls/s, p99 48 ms/60 ms/591 ms/1.52 s; about one core in total; slows with uptime; at 50+ agents fail-closed refuses up to 31% on wardryx decide timeouts (tokenfuse#319). |
| Power pulled under load (two agents calling every 0.5 s, the record plane sealing) | 2026-09-23 | forge | `go-to-market-2026-09/evidence/forge-appliance-2026-09-23/` `logs/pc-1.log`, `logs/pc-2.log`, `logs/powercut-before.txt`, `logs/after-cut.txt` | 51 s from the last 200 to the next, unplugged time included; boot 27.2 s; every container back by itself; a new pack of 3482 records VERIFIED and the pre-cut pack VERIFIED; 3504 event lines, 0 torn; `.env` and the Cloud's month kept; the notifier resumed from its offsets, no mail sent twice. One cut, one box. |
| A customer agent in a MANAGED cluster (GKE 1.35.8, one e2-medium), Tailscale as a userspace native sidecar, no node access | 2026-09-23 | GCP `stack-k8s-gcp` europe-west3-a | `go-to-market-2026-09/evidence/forge-appliance-2026-09-23/` `agent-job.yaml`, `logs/agent-gke.log` | 17 x 200 then 402 on call 18 (50 ms) to the real Anthropic API through the box; `TS_KUBE_SECRET=""` needed or the sidecar crash-loops on RBAC. EKS not run. |
| Teardown of the GKE cluster, GCP verified empty | 2026-09-23 | GCP `stack-k8s-gcp` | `go-to-market-2026-09/evidence/forge-appliance-2026-09-23/` `SUMMARY.md` | clusters, instances, disks, addresses, forwarding rules: 0 by direct query. Nothing was created in AWS. |
| A customer agent in a MANAGED EKS cluster (Kubernetes 1.36, one t3.medium, AL2023), the same userspace Tailscale sidecar Job; then teardown | 2026-09-23 | AWS `stack-k8s` profile eu-central-1 | `go-to-market-2026-09/evidence/forge-appliance-2026-09-23/` `logs/agent-eks.log`, `logs/agent-eks-unitcap.log`, `SUMMARY.md` | control plane ACTIVE in about 10 min, node Ready 3 min later; the pod reached the box in 17 s; the unit's monthly cap refused call 1 (correct, already spent), then 17 x 200 and 402 on call 18 in 49 ms; node group and cluster deleted, then EKS, instances, volumes, EIPs, ENIs and load balancers 0 by direct query. |

## The gateway against a real provider, proven

| What | When | On what | Artifact | Scope |
|---|---|---|---|---|
| The released `tokenfuse:v0.4.4` image in `enforce` mode in front of `api.anthropic.com` (Messages door), a half-cent per-run budget, the breaker refusing the third call with `402 budget_exceeded` and exporting two `breaker_tripped` events with a `prev_hash` chain | 2026-09-12 | this Mac, Docker Desktop 29.7.2, linux/aarch64, the image pulled by tag | `go-to-market-2026-09/evidence/1.0/r3-anthropic-2026-09-12/` (`SUMMARY.md`, `calls.log`, `hdr-*.txt`, `body-*.json`, `events.ndjson`) | One door, one provider, one run, four calls, `claude-haiku-4-5-20251001`, about USD 0.0004. A first attempt sized its budget so that eight calls fit under it and saw eight 200s, which is recorded, not a defect. Not the OpenAI door (unreleased on main at that hour), not streaming, not a cluster |
| The OpenAI door (`/v1/chat/completions`, tokenfuse main at `a83f747`, built locally, unreleased) in `enforce` mode in front of OpenRouter: call 1 served at `x-fuse-price: fallback`, call 2 refused `402 budget_exceeded` in the OpenAI error shape, three `breaker_tripped` events chained | 2026-09-12 | this Mac, native binary, `cargo build --release` 2 min 43 s | `go-to-market-2026-09/evidence/1.0/r3-openrouter-openai-door-2026-09-12/` | One door, one provider, one run, `openai/gpt-4o-mini`, about USD 0.00006. Finding: the price book does not know OpenRouter's vendor-prefixed ids, so the call was fallback-priced at about 100x; a first attempt with a real-price budget was refused before any call, which is the documented estimate-then-settle behaviour. Not streaming, not the released image |

| The first tag through the wave-1 release mechanics: `tokenfuse` `v0.5.0` (`da324a9`), release run 34724668614 green on every job. From a machine that built nothing: `sha256sum -c` OK on a downloaded binary against the released `SHA256SUMS`; `gh attestation verify tokenfuse-cloud-aarch64-apple-darwin -R TAIPANBOX/tokenfuse` and `gh attestation verify oci://ghcr.io/taipanbox/tokenfuse:v0.5.0 -R TAIPANBOX/tokenfuse` both returned one SLSA provenance v1 statement (16 subjects for the release assets; the image by manifest-list digest) signed by `.github/workflows/release.yml@refs/tags/v0.5.0` through `token.actions.githubusercontent.com`. The release page carries `SHA256SUMS.sigstore.json` (Sigstore bundle v0.3), `sbom.spdx.json` (SPDX-2.3, 1106 packages), `sbom.cyclonedx.json` (1.7, 1116 components) and `provenance.intoto.jsonl`. The registry carries `v0.5.0` and `v0.5.0-cluster` and no new moving tag. | 2026-09-13 | GitHub Actions (the run), this Mac (the verification, gh 2.100.0) | `gh run view 34724668614 --repo TAIPANBOX/tokenfuse`; the release page; scratchpad `wave1/v050/` for the downloaded files | The `cosign verify-blob` half of the README's recipe was NOT run here: cosign is not installed on this machine, so the Sigstore bundle on `SHA256SUMS` is present and unverified from outside the runner; `gh attestation verify` covers the same file through the provenance statement. The July `latest` and `cluster` tags were deleted on 2026-09-13 on the owner's word (four package versions from 2026-07-15, `sha-8756b8b`/`cluster-8756b8b`, each carrying only its moving tag and its own sha tag; `gh api -X DELETE user/packages/container/<image>/versions/<id>`); @measured `curl .../v2/taipanbox/<image>/tags/list` 2026-09-13 afterwards: no `latest` and no bare `cluster` on any of the three images, `v0.5.0` present, and `docker pull ghcr.io/taipanbox/tokenfuse` answers `not found`, which is route A working. |

## The 1.0 proving run, proven (RELEASE-1.0-PLAN-2026-09-12 section 5.3)

One row per scenario group per rig. R0 is this Mac; the four R1 scenarios run
here ahead of R1 count for the behaviour, not for R1, until a Linux box under
systemd repeats them. `SUMMARY.md` in each rig's evidence directory carries that rig's findings
(seven on R0, seven on R1, fourteen on R2 GCP, three on R2 AWS, seven on R3; the 2026-09-14 runs on the 1.1 set, one finding each, both pre-existing).

| What | When | On what | Artifact | Scope |
|---|---|---|---|---|
| INS-1: the README `docker run` of `tokenfuse:v0.5.0` on a machine that had only v0.4.4, `/healthz` 200, a 0.02 USD run refused on its third call with `402 budget_exceeded` (spent 0.021) | 2026-09-12 | this Mac, Docker Desktop, the image pulled by tag (linux/arm64, digest `dcf35d5…`) | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/ins-1-pull.log`, `ins-1-run.log` | one door, the stub upstream. Finding: the container's `--version` prints `tokenfuse  ()` (tokenfuse #275, open) |
| INS-4 cold: `stack-up --with-delegation --with-finops` from an empty workspace, empty `CARGO_HOME`/`GOMODCACHE`/`GOCACHE`/npm cache, fresh `TAIPAN_HOME`: 12 repos cloned, 15 binaries built (399 crates downloaded, 353 compiled, 100 Go modules), **325 s** to every plane answering, clean stop, exit 0 | 2026-09-13 | this Mac, Apple Silicon, stack-up `8700c3b` | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/ins-4-cold-stack-up.log` and `.up` | tools ON this time (2026-09-01 measured 221 s with `--no-tools`). Warm only in the toolchains themselves (rustup, Go, node, python3). The warm run the same night (`ins-4-stack-up.log`) ended when FAIL-2 killed wardryx: the launcher stops the whole stand when one plane exits, by design |
| RUN-2: the wrong door for the process's wire is refused `400 wire_mismatch` before anything is reserved; the right door on the same run still has the full budget | 2026-09-12 | this Mac, the v0.5.0 container from INS-1 | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/run-2.log` | docs/26's claim, one process, one run |
| RUN-4: `bank-in-a-box` night incident, the seven-check scoreboard **7/7 PASS** (402 within budget, breaker chain intact, events valid each against its own schema version, Idryx `runaway_agent` HIGH, Engram `why()` provenance, Qryx quantum at-risk, outcomes report) and its self-test still trips all 8 broken inputs; `offline-only` OK | 2026-09-12 | this Mac, siblings tokenfuse `d4e9ddd`, idryx `bfd7c7b`, qryx `25cf2a5`, engram `f16b59a`, bank-in-a-box `3eaf073` + PR #4 | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/run-4-scoreboard.log`, `run-4-events.ndjson` | the first attempt could not start the stand's gateway (no `TOKENFUSE_ALLOW_STUB`, a precondition since 2026-08-05) and the second failed the schema check (pinned to v0.1): both fixed in bank-in-a-box #4, open |
| RUN-5 (R1 scenario, ahead): hierarchical runs; with wardryx enforcement on, a call without `x-fuse-agent-id` is `400 identity_required`; with it, the parent (0.02) serves itself and child a, and child b is `402` with the parent spent 0.021 | 2026-09-13 | this Mac, stack-up's gateway (main, 0.0.1-dev), stub upstream | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/run-5-hierarchy.log` | one parent, two children; the R1 run is still owed |
| RUN-6 (R1 scenario, ahead): the same over-budget run in shadow (all four calls forwarded, ledger at 210 %) and in enforce (402 on call 3, `breaker_tripped` on the bus, trace `budget_exceeded`) | 2026-09-13 | this Mac, `~/.taipan/bin/tokenfuse-gateway` (main, 0.0.1-dev), stub upstream, wardryx up | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/run-6-shadow-then-enforce.log`, `run-6-run.sh` | **finding**: shadow records no would-be refusal for the run budget (no `x-fuse-would-block`, no event, trace `allow`); README line 381 promises it does. T3, open, fix proposed in SUMMARY.md |
| POL-3 (R1 scenario, ahead): scopyx's gates in order, address rule `deny_address`, allowed origin fetched, denied origin `deny_policy`, an allowed origin redirecting into a denied one refused on the target host, robots `deny_robots` (github.com `/search$`), PDP killed `deny_policy_unreachable`; each on the bus as `web_blocked` high | 2026-09-13 | this Mac, `~/.taipan/bin/scopyx` and `wardryx` on private ports with `policy-r0.yaml` | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/pol-3-scopyx-gates.log`, `pol-3-run.sh`, `stand.sh` | five gates, one agent identity, one policy |
| INT-1: nine framework recipes (LangChain, OpenAI Agents SDK, PydanticAI, CrewAI, AutoGen, Semantic Kernel, LiteLLM, Haystack, Claude Code documented) against Ollama `qwen2.5:3b` through both doors of the v0.5.0 image: **12 door runs, 12 refusals** with each framework's own exception; CrewAI's `extra_headers` reaches the gateway through LiteLLM | 2026-09-13 | this Mac, Python 3.14.7 (CrewAI in `python:3.13-slim`), two v0.5.0 containers | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/int-1-recipes.log`, `int-1-crewai.log`; the recipes are tokenfuse #276 | prices are the fallback book (`x-fuse-price: fallback`); the CI matrix is not run (the stub refuses the OpenAI wire by design) |
| INT-3: an MCP client on scopyx `browse` (allowed fetched, denied refused) and on the tokenfuse mcp-broker with wardryx `decide()` on `tools/call`: `shell_exec` refused for the mockryx identity with wardryx's reason (JSON-RPC -32004) and provably absent from the upstream's log, `read_clock` forwarded, a call with no identity refused | 2026-09-13 | this Mac, the broker from `~/.taipan/bin/tokenfuse-gateway mcp-broker`, a 30-line echo MCP upstream | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/int-3-mcp-layer.log`, `int-3-run.sh`, `echo_mcp.py` | the client was JSON-RPC by hand, not Claude Code or Cursor. Finding: broker and scopyx do not chain (no upstream credential) |
| SUP-1: the README's own verify commands from a machine that built nothing: `cosign verify-blob` OK on `SHA256SUMS`, `sha256sum -c` OK, `gh attestation verify` OK on the asset and on four images, one flipped hex digit fails | 2026-09-12 | this Mac, cosign v3.1.3 in a container, gh 2.100.0 | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/sup-1.log` | tokenfuse v0.5.0 only; the other eleven repos' mechanics are proven by their first tag, not yet cut |
| FAIL-2 (R1 scenario, ahead): wardryx killed under a running gateway and scopyx: tokenfuse forwards (200, `x-fuse-wardryx: allow`) and records `dependency_failed` high `allowed_ungoverned` `failmode=Open applied`, `/v1/policy-plane` `falling_back: true`; scopyx refuses `deny_policy_unreachable` and records `web_blocked` high | 2026-09-13 | this Mac, the three planes started by `fail-2-run.sh` outside stack-up (stack-up itself stops the stand when a plane dies) | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/fail-2-pdp-down.log`, `fail-2-run.sh` | within the gateway's 3 s decision cache the cached allow is reused and no outage is noticed; the run shows both sides of the TTL |
| INS-2 (R1a): `stack-single` by its README one-liner on a fresh GCP `e2-standard-2`, installer md5 checked against main: **112 s, exit 0, 21 ok, 0 fail**, 8 images pulled, nothing built; the same installer again on the same box: 33 s, 21 ok, no container recreated, 15 volumes unchanged, .env and compose.yaml untouched; a second fresh box: 116 s, 21 ok | 2026-09-13 | GCP europe-west3-a, Ubuntu 26.04.1, Docker 29.1.3, stack-single `87a77ac`; both boxes destroyed the same hour | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/ins-2-run1-x86.log`, `ins-2-run2-x86-rerun.log`, `ins-2-run3-x86-fresh-box.log`, `logs/` (full install logs, passwords redacted) | pins as on main that day (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0, console v0.1.2). Finding: the gateway exported no events and three profiles could not write their volumes (stack-single #41), invisible to all 21 checks |
| INS-3 (R1b): the same install on arm64, GCP `t2a-standard-2`: **83 s, exit 0, 21 ok**; every one of the 9 running images resolved `linux/arm64`, same manifest-list digests as x86 | 2026-09-13 | GCP europe-west4-a, Ubuntu 26.04.1 arm64; destroyed after 22 min | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/ins-3-arm64.log`, `logs/r1b-install-1.log` | closes "arm64 running on arm64" below at the cheapest shape; no cluster on arm64 yet |
| INS-7 (R1a): the README install blocks and first commands of qryx, idryx and trailryx run literally on the clean box: release tarballs and binaries downloaded and `sha256sum -c` OK, `qryx version` v0.3.1, `idryx version` v0.3.1, `idryx detect` on the day's real bus raised `runaway_agent`, trailryx's day sealed under `--trust-domain taipanbox.dev` (4 lines, 7 records), packed and **VERIFIED** by the v0.1.2 release verifier | 2026-09-13 | the R1a box | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/ins-7-readme-commands-r1.log` | two trailryx README lines assume a host binary (the image has no entrypoint) and omit `--trust-domain`; the wrong-domain run refuses loudly and moves no cursor (finding 6 in SUMMARY.md) |
| UPG-1 and UPG-5 (R1a): the stand's Parquet trace written by tokenfuse 0.4.4, read by 0.5.0 after moving `TOKENFUSE_IMAGE` in .env: `outcomes` and `sql` identical to the microdollar, the columns the old writer lacked read as empty; `docker compose up -d` converged in 7 s recreating only the gateway, volumes and policy-db untouched, 21 checks on the upgraded stand | 2026-09-13 | the R1a box | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/upg-1-5-r1.log` | 8 calls of trace, not a day; the v0.5.0 image prints `tokenfuse  ()` for `--version` (tokenfuse #275 fixes the next tag) |
| UPG-3 (R1a): records sealed by the `trailryx-node:v0.1.2` image, verified by `trailryx-verify` built from main `8f84c4c` on the box: **VERIFIED**, completeness proof Full when the main node reads the 0.1.2 data dir | 2026-09-13 | the R1a box, rustup stable, 44 s build | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/upg-3-r1.log` | one pack of 7 records |
| RUN-5, RUN-6, POL-3, FAIL-2 on R1a (the R1 runs of the four scenarios R0 ran ahead): RUN-5 child b `402` on the parent's budget; RUN-6 enforce 402 on call 3 with `breaker_tripped` on the bus, shadow forwards with no would-block signal (the R0 finding on the released v0.5.0); POL-3 five gates on the stand's scopyx v0.1.2 with wardryx v0.1.0; FAIL-2 with the stand's `TOKENFUSE_WARDRYX_FAILMODE: closed`: **403 wardryx_denied** and `dependency_failed` high, scopyx `deny_policy_unreachable`, both recover | 2026-09-13 | the R1a box, stack-single's compose with a run override (stub upstream, events path) | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/run-5-6-r1.log`, `pol-3-r1.log`, `fail-2-r1.log` | the events path had to be supplied by the override (finding 1); the stand's gateway is fail-closed by configuration where stack-up's default was fail-open, so both failmodes are on record |
| POL-5 (R1a): qryx v0.3.1 `image` on the stand's three images (350/146/146 findings), `bin` on four release binaries, `tls` on the console by name (ECDSA-256 quantum-vulnerable, correct), `scan --format cbom` of tokenfuse source **VALID CycloneDX 1.6** (jsonschema 4.19.2 against the 1.6 schema), signed ed25519 evidence **VERIFIED** and a one-number tamper refused with "digest mismatch" | 2026-09-13 | the R1a box | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/pol-5-r1.log` | **finding**: `image --format cbom` yields 9 schema errors (primitives `encryption`, `key-exchange`; asset type `library`), see `finding-qryx-cbom-not-cyclonedx-1.6.log`; fix owed in qryx |
| REC-3 (R1a): `trailryx-demo --runs 2` from main: 4 keys destroyed over 4 payloads, the same pack VERIFIED again byte for byte, payloads unreachable in storage; the kept pack VERIFIED by the v0.1.2 release verifier | 2026-09-13 | the R1a box | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/rec-3-r1.log`, `logs/rec-3-demo-full.out` | erasure is a library and the demo path; `trailryx-node` has no erase command (named gap) |
| INT-5 (R1a), the API half: inside the stand's console container, Engram 2.4.1 episode, fact with source, `why()` with a non-empty chain, recall | 2026-09-13 | the R1a box | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/int-5-r1.log` | **finding**: the console half fails, `engram-mcp` in the console image dies for want of the `mcp` extra and the descriptor has no `services.engram`; Engram's first use fetched its embedding model from the HF Hub |
| SUP-3 (R1a): tcpdump of every outbound SYN and DNS query for the scenario set, read name by name: image registries, GitHub releases, the scenarios' own targets, apt and rustup (the operator's), the GCE guest agent (the image's), and **api.ipify.org asked by install.sh on every run**; nothing else | 2026-09-13 | the R1a box, two captures (a 4 min gap where the first died with its SSH session) | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/sup-3-egress-r1.log`, `capture/*.pcap`, `sup3-dns.txt`, `sup3-syns.txt` | the HF Hub fetch of INT-5 fell in the gap and is in `int-5-r1.log` only |
| FAIL-5 (R1a): the record plane on a 64 KiB tmpfs: `journal: io: No space left on device`, exit 2, no cursor written, the store reads back 0 records; the same input on a real disk seals 400; identical under v0.1.2 and main | 2026-09-13 | the R1a box | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13/fail-5-r1.log` | one failure mode (ENOSPC at seal time), not a full disk mid-segment |
| SUP-4: Scorecard published for all 21 repositories, read from api.scorecard.dev after the tag-object pins were fixed and every repository's push run uploaded: **Security-Policy 10 everywhere**; Signed-Releases tokenfuse 3 (v0.5.0 signed with provenance, the two earlier releases in the window not), trailryx, idryx, qryx, agent-stack-go, mockryx 0 (no release since wave 1), terraform-provider 8; Pinned-Dependencies 10 in five repositories and 7 to 8 in most others (container base images not pinned by digest); Token-Permissions 0 in ten repositories at the first reading (a top-level `contents`/`packages: write` in release.yml, or a workflow with no top-level `permissions:`), **10 in all 21 by 13:13Z** after the ten `ci/token-permissions-read-only-at-the-top` merges and their push runs; every repository's score rose (tokenfuse 4.8 to 5.5, idryx 5.9 to 6.7, wardryx 5.8 to 6.6) | 2026-09-13 | api.scorecard.dev, each repository's `scorecard.yml` push run after the pin merge | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/sup-4-scorecard-sunday.log` (the table, the check details for tokenfuse, idryx and trailryx, and the reading) | the score climbs one signed release at a time (Scorecard reads the last five), so "Signed-Releases 10" is not a figure a 1.0 tag reaches on its own; the Token-Permissions fix landed the same afternoon (ten PRs, every one proven on its workflow's pull-request path; the tag-only jobs are proven by the next tag). Until this afternoon every upload had been refused as an "imposter commit" (the C21 story) |
| INT-2: Claude Code itself (2.1.270, the owner's plan, OAuth, no API key) pointed at the v0.5.0 gateway by `ANTHROPIC_BASE_URL`, run id and budget in `ANTHROPIC_CUSTOM_HEADERS`: call 1 answered by api.anthropic.com through the proxy and priced 0.176436 USD (haiku 4.5, 140,373 cache-creation tokens), call 2 **refused 402** before it left the box (spent 0.176 + estimate 0.329 > 0.40), which the CLI printed verbatim and stopped on; `breaker_tripped` on the bus, trace `allow` then `budget_exceeded` | 2026-09-13 | this Mac, `int-2-run.sh 0.40`, the v0.5.0 container in front of api.anthropic.com | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/int-2-claude-code-run.log`, `int-2-run.sh` | a 0.30 budget refused the FIRST call: the CLI asks for 32,000 output tokens, so the up-front estimate is 0.328 USD. **Finding**: the gateway prices the 1-hour cache write at the 5-minute rate (0.176 against Claude Code's own 0.282 for the same call), tokenfuse#282 |
| INS-5 (R2 GCP): `deploy-gcp.sh` three times. Run 1 from bare machines went RED (a fresh cluster's gateway and console in `CreateContainerConfigError`, the `stack-keys` Secret missing `gateway_admin`; verify 9/2/3); run 2 over the same cluster with the fixed installer added the key and passed verify **15/0/1**, security-tests **27/0/3**, in 8 min 20 s; run 3 from an empty namespace took the fresh path and passed **15/0/1**, **27/0/3**, in 8 min 39 s. Every image pulled, nothing built | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `logs/deploy-1.log`, `logs/deploy-2b.log`, `logs/deploy-3.log`, `ins-5-verify-after-run-2.log` | three deploys on one terraform'd cluster; the installer fix is stack-k8s#73, not yet merged |
| INS-6 (R2): the placeholder trust domain left standing on purpose. The seal did NOT refuse: 9 records sealed under `agent://set-me.invalid/...`, `foreign_trust_domain 0`, pack VERIFIED; the callers that carry their own domain refused as foreign; no deploy-time gate existed. `verify.sh` now fails on the placeholder (red on this cluster, green after `--trust-domain taipanbox.dev`) | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `ins-6-bus-under-placeholder.log`, `ins-6-seal-under-placeholder.log`, `ins-6-verify-red-on-placeholder.log` | the check is in stack-k8s#73 |
| RUN-1 (R2, the `/v1/messages` door only): a 0.0006 USD run against the real Anthropic API through the cluster gateway, 10 calls at 0.000034, call 11 **402** with the wire body (`budget_exceeded`, spent 0.00034); `/v1/runs` agrees; `breaker_tripped` on the bus; the record-seal routine sealed it under `taipanbox.dev` and `trailryx-verify` said VERIFIED | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `run-1-messages-door.log`, `rec-1-routines.log` | the `/v1/chat/completions` half did not run: the pinned gateway is v0.4.4, the door is v0.5.0 |
| RUN-8 (R2): the 34-burst with per-agent rights against the shipped policy, **34 of 34 decisions right** (14 deny, 8 hold, 12 allow) in 16 ms; 3000 decisions at 16, 32, 64, 128, 256 concurrent callers: **21 295 to 27 210 decisions/s**, p99 2.4 to 44.2 ms, 0 errors, 0 5xx, no cliff; all 15 034 decisions on the bus at 392 bytes each | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `run-8-wardryx-load.log` | one wardryx pod, caller on the pod network, Postgres store; not the 2026-07 dedicated-core numbers |
| POL-1 (R2): the eight-scenario mockryx catalog with `--fail-on-skip` and a real provider key: **4 passed**, 5 gaps each explained (injected-page not configured as the plan says; heraldyx and verdryx not deployed; `runaway-budget` names model `claude-haiku`, which Anthropic answers 400; the wardryx `policy_deny` for reaction-chain was written and the drill's watcher missed it inside 10 s on the RWX volume) | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `pol-1-drills-fail-on-skip.log` | mockryx v0.1.1 image, the catalog from the repo's `scenarios/` mounted as a ConfigMap |
| POL-2 (R2, the API half): hold, admin grant, token presented twice: **allow, allow** by default; **allow, then hold** (`already redeemed once`) under `WARDRYX_APPROVAL_SINGLE_USE=1`, flipped live and back; both behaviours of invariant 5 recorded | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `pol-2-approval-required.log` | the console's passkey ceremony is his half and did not run; the 1.0 default is his decision |
| POL-4 (R2, partly): idryx `detect` on the R2 bus (613 alerts, a `critical data_exfiltration` for the drill notetaker), `bom` over the bus (304 agents), a qryx code scan of the eight recipes (19 sources, 3 AI-usage assets); a crafted 12-record Okta export raised 0 alerts | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `pol-4-idryx-detect-bom-inventory.log` | `ai-inventory` is not in idryx v0.3.0 and `--format ai-inventory` not in qryx v0.3.0: the three-source inventory could not run on the pinned set; the 27-detector count was not counted |
| REC-1 (R2): the five routines fired by hand: qryx-trend 6 s (scanned an empty directory: finding), idryx identity-sweep 3 s (6 alerts from the bus), record-seal 16 s (RUN-1 sealed, 15 037 wardryx and 8 mockryx lines refused as foreign, cursor logic as trailryx#50), verdryx quality-drift skipped (no eval yet), mockryx drills failed on runaway-budget | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `rec-1-routines.log` | the console was not opened to show them |
| REC-2 (R2, offline half on the Mac): the sealed pack copied off the cluster, `trailryx-verify` from the published `trailryx-node:v0.1.2` image with `--network none`: **VERIFIED**; one byte flipped at 2200 and again at 4380: **NOT VERIFIED**, `[BROKEN] chain-within-segment`, `history-root`, five `index-root` lines, exit 1 | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `rec-2-verify-then-flip.log` | no root signature and no witness in this pack, as its own `[weak]` lines say |
| INT-4 (R2): terraform-provider-taipan **from the public Registry** (`v0.1.1`, self-signed key A10CC414FD1450D6): `taipan_budget`, `taipan_wardryx_policy`, `taipan_agent_passport` applied against the R2 planes, a hand-set budget imported, plan after apply and import **No changes**; `taipan_unit_budget` from a local build of main applied and clean | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `int-4-terraform-provider.log` | the Registry release has three resources, the README says four; the passport resource does not create its output directory |
| FAIL-1 (R2): provider errors mid-run. (A) an in-cluster stub answering 200, 429, 503, 529, 200 in one budgeted run: each error forwarded with cost 0, spent unchanged, reserved 0, the run continued, **no double-settle**, and no event on the bus for any of the three. (B) unreachable upstream: `502 upstream_error`, `dependency_failed` (high) on the bus, heraldyx picked it up within 2 s and attempted delivery | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `fail-1-provider-errors-mid-run.log` | `alert_sent` needs a real SMTP server and was not proven; heraldyx ran with a placeholder host |
| FAIL-3 (R2): SIGKILL of the gateway mid-stream from the node (from inside the container a PID-namespace init ignores it: recorded): the client got a truncated stream (curl exit 18), container exit 137, ready again in 28 s; the interrupted reservation died with the process (no settle, no double-settle); the gateway's ledger came back **empty**, the control plane kept its rows | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `fail-3-kill-9-mid-stream.log` | one replica, HA compiled out: no raft, no re-election to time |
| FAIL-4 (R2): a freeze written through the admin API into the Postgres store, `verify.sh --freeze` restarted the policy plane: PDP **deny before, deny after**, `the freeze survived a policy-plane restart`, verify 16/0/1 | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `fail-4-freeze-survives-restart.log` | one wardryx pod |
| R2 GCP teardown: `teardown.sh --yes`, `Destroy complete! Resources: 15 destroyed`, sweep clear; direct query 0 instances, 0 disks, 0 addresses, 0 forwarding rules, 0 target pools, 0 health checks, terraform state 0. Billed 1.48 h at USD 2.0387/h, about **USD 3.02**; model spend under USD 0.01 | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, the pinned set as stack-k8s main pins it (tokenfuse v0.4.4, wardryx v0.1.0, idryx v0.3.0), driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13/` `teardown.log` | the four `default-*` firewall rules and the project's compute service account are GCP's own |
| INS-5 (R2 AWS, the plan's "AWS after the GHCR migration"): two clusters. Cluster 1 run 1 from bare machines **15/0/1, 27/0/3** in 11 min 36 s (`created secret stack-keys`, six keys); its run 2 went **RED at 23 s** (`install-aws.sh` minted a fresh k3s token, server-1 refused to start: `bootstrap data already found and encrypted with different token`). Cluster 2, fixed installer, no hand touches: run 1 **15/0/1, 27/0/3** in 11 min 33 s; run 2 over it **15/0/1, 27/0/3** in 7 min 32 s, `reusing the token this cluster was created with`. Every image pulled, nothing built | 2026-09-13 | AWS account 603770384533 (profile `stack-k8s`), 5 x `c7a.2xlarge` in eu-central-1a, k3s + Calico + Longhorn, stack-k8s main `05d9086` (the #73 fix in from the first command), same pinned set as the GCP leg, driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-aws-2026-09-13/` `logs/deploy-1.log`, `logs/deploy-2.log`, `ins-5-run-2-k3s-journal.log`, `logs/deploy-3.log`, `logs/deploy-4.log` | the token fix is stack-k8s#74, open, his merge |
| RUN-1 (R2 AWS, messages door): the same 0.0006 USD run against the real Anthropic API through the AWS gateway: **10 calls at 0.000034, call 11 `402 budget_exceeded`** (spent 0.00034), ledger agrees; sealed by the record-seal routine under `taipanbox.dev`. Identical to GCP to the microdollar | 2026-09-13 | AWS account 603770384533 (profile `stack-k8s`), 5 x `c7a.2xlarge` in eu-central-1a, k3s + Calico + Longhorn, stack-k8s main `05d9086` (the #73 fix in from the first command), same pinned set as the GCP leg, driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-aws-2026-09-13/` `run-1-messages-door.log`, `rec-1-routines.log` | cluster 1, before the token incident; the `/v1/chat/completions` half did not run (gateway pinned v0.4.4) |
| RUN-8 (R2 AWS): the 34-burst **34 of 34 right**; 3000 decisions at 16, 64, 256 callers: **28 300, 39 218, 42 732 decisions/s**, p99 2.1, 10.1, 30.3 ms, 0 errors, 0 5xx; 9042 lines on the wardryx bus afterwards | 2026-09-13 | AWS account 603770384533 (profile `stack-k8s`), 5 x `c7a.2xlarge` in eu-central-1a, k3s + Calico + Longhorn, stack-k8s main `05d9086` (the #73 fix in from the first command), same pinned set as the GCP leg, driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-aws-2026-09-13/` `run-8-wardryx-load.log` | cluster 1, one wardryx pod; the probe pod again saw the RWX file late (39 lines five seconds after 9034 decisions) |
| FAIL-4 (R2 AWS): a freeze written through the admin API, `verify.sh --freeze`: PDP **deny before, deny after** the policy-plane restart, verify 16/0/1 | 2026-09-13 | AWS account 603770384533 (profile `stack-k8s`), 5 x `c7a.2xlarge` in eu-central-1a, k3s + Calico + Longhorn, stack-k8s main `05d9086` (the #73 fix in from the first command), same pinned set as the GCP leg, driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-aws-2026-09-13/` `fail-4-freeze-survives-restart.log` | cluster 1 |
| R2 AWS teardown, twice: `teardown.sh --yes`, `Destroy complete! Resources: 21 destroyed`, sweep `Nothing left` both times; direct query 0 instances, 0 volumes, 0 elastic IPs, 0 load balancers, 0 non-default VPCs, terraform state 0. Billed 0.65 h + 0.42 h at USD 2.4328/h, about **USD 2.60** | 2026-09-13 | AWS account 603770384533 (profile `stack-k8s`), 5 x `c7a.2xlarge` in eu-central-1a, k3s + Calico + Longhorn, stack-k8s main `05d9086` (the #73 fix in from the first command), same pinned set as the GCP leg, driven from the Mac | `go-to-market-2026-09/evidence/1.0/r2-aws-2026-09-13/` `teardown-1.log`, `teardown-2.log` | security group `mail-sink` and key pairs `mail-sink`, `ite-test` predate the run (2026-08-02 and 2023), cost nothing, left alone |
| RUN-1 (R3, six upstreams through both doors): Anthropic haiku 4.5 (Messages), Ollama qwen2.5:3b (Messages and OpenAI), Vertex `google/gemini-2.5-flash-lite` (OpenAI), Bedrock `openai.gpt-oss-20b-1:0` (OpenAI), OpenRouter `openai/gpt-4o-mini` (OpenAI): every run served its calls and was refused with **402 and the wire-correct body for its door**, `/v1/runs` agreeing with the last `x-fuse-spent-usd`, `breaker_tripped` on the bus. Anthropic: 10 x 0.000034, 402 at 0.00034, the fourth identical measurement of the day | 2026-09-13 | this Mac, six `ghcr.io/taipanbox/tokenfuse:v0.5.0` gateways under Docker, one per upstream, enforce mode | `go-to-market-2026-09/evidence/1.0/r3-providers-2026-09-13/` `anthropic-messages-door.log`, `ollama-messages-door.log`, `ollama-openai-door.log`, `vertex-gemini-openai-door.log`, `bedrock-openai-door.log`, `openrouter-openai-door.log` | every non-Anthropic id fallback-priced (the book knows none of them); no real OpenAI key, OpenRouter stood in on the same wire; Nova is not served as OpenAI wire by Bedrock |
| RUN-3 (R3, streaming): Messages door, one `message_start` and one `message_delta`, settle equal to usage (Anthropic 24 in + 27 out = **0.000159**, ledger 0.000159). OpenAI door on four providers, three cases each: no `stream_options` -> **exactly one** usage chunk (injected); caller-set `include_usage: true` -> exactly one (not doubled); settle equal to usage at the fallback price to the microdollar (Vertex 2205 = 2205, OpenRouter 2145 = 2145). Caller-set `include_usage: false` -> **0 usage chunks and a ZERO settle** on Ollama and Bedrock (0 tokens, 0 microusd in the trace, `spent_usd` unmoved), the documented estimate settle did not happen: **tokenfuse#283**, T3 | 2026-09-13 | this Mac, six `ghcr.io/taipanbox/tokenfuse:v0.5.0` gateways under Docker, one per upstream, enforce mode | `go-to-market-2026-09/evidence/1.0/r3-providers-2026-09-13/` `ollama-openai-door.log`, `ollama-openai-door-run3-trace.log`, `bedrock-openai-door.log`, `vertex-gemini-openai-door.log`, `openrouter-openai-door.log` | the third case FAILED RUN-3's "settle matches" on v0.5.0; fixed on main the same day (tokenfuse#284, `ae72672`, invariant 43), no released image carries it yet; Vertex and OpenRouter send usage regardless of the flag and settle right |
| R3 seal: `trailryx-node events` over the six gateways' event files under `taipanbox.dev`, every line mapped, `foreign_trust_domain 0`, 5 segments, 15 records, `pack-r3.pack` **VERIFIED** | 2026-09-13 | this Mac, six `ghcr.io/taipanbox/tokenfuse:v0.5.0` gateways under Docker, one per upstream, enforce mode | `go-to-market-2026-09/evidence/1.0/r3-providers-2026-09-13/` `rec-seal.log` | OpenRouter's event landed after the seal and is on the bus unsealed |
| R3 cost: Anthropic under USD 0.002, OpenRouter USD 0.0000141 by its own meter, Vertex under 0.001 on credits, Bedrock under 0.001, Ollama free: **under USD 0.01** against a go of 5 to 15; nothing created on any cloud account (the Bedrock bearer token is derived from IAM credentials by a stdlib Go presigner and expires by itself) | 2026-09-13 | this Mac, six `ghcr.io/taipanbox/tokenfuse:v0.5.0` gateways under Docker, one per upstream, enforce mode | `go-to-market-2026-09/evidence/1.0/r3-providers-2026-09-13/` `SUMMARY.md` | the two 7-day keys of 2026-09-13 should be revoked after the run; both passed through chat |
| Wave 3 tags: **trailryx, idryx, qryx, wardryx, scopyx v1.0.0**, each on the squash-merge of its release PR (trailryx#74 `d68a44f`, idryx#84 `9543564`, qryx#56 `49f6574`, wardryx#55 `5996e1f`, scopyx#40 `51a8c39`), `compat/1.0.json` status `frozen at 1.0.0 (2026-09-13)` in all five, `docs/releases/v1.0.0.md` in all five; `release.yml` on each tag published every image (`trailryx`, `trailryx-node`, `idryx`, `qryx`, `wardryx`, `scopyx`, `scopyx:-chromium`, amd64+arm64, signed and attested) and the Release pages of trailryx (19 assets), idryx and qryx (10 each) with the notes put on them by `gh release edit` | 2026-09-13 | GitHub Actions on the five tag pushes, driven from this Mac | the five Release pages; `gh run view` 34779434784 (trailryx), 34779167368 (idryx), 34779172800 (qryx), 34779178973 (scopyx), 34779184945 (wardryx) | **finding**: wardryx's and scopyx's tag-only `release page` job failed at its first step (`anchore/sbom-action` writes into `dist/`, which no earlier step creates in an image-only repository), after both images were out: no Release object for either v1.0.0 yet; fix and a dispatch path for an existing tag in wardryx#56 and scopyx#41, the tag not moved. The `release page` job was the half C17 says only a tag proves, and it did |
| UPG-2 (R0): a wardryx **0.1.0** deployment on Postgres upgraded in place to the released **1.0.0** image, same volume, database and `WARDRYX_APPROVAL_SECRET`: the policy written under 0.1.0 through `PUT /v1/policies` restored (`restored 1 policy(ies) from the store`), read back unchanged, `/v1/status` unchanged, the same deny with the same reason and `policy_version`; both 0.1.0 holds listed; the token 0.1.0 minted **allowed** on its first presentation under 1.0.0 and **held** on its second (`already redeemed once`, the 1.0 default); `wardryx replay` over the whole file: 7 decisions, 2 reproduced, 1 approval-decided, 3 `unreadable` (recorded by 0.1.0 before the emitter carried the question, counted by name), 1 diverged | 2026-09-13 | this Mac, Docker Desktop, `postgres:16-alpine`, `ghcr.io/taipanbox/wardryx:v0.1.0` then `:v1.0.0` (arm64) | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/upg-2-wardryx.log`, `upg-2-run.sh` | **finding**: the diverged row is the single-use refusal itself, a hold whose reason only the unrecorded token explains (invariant 15), which replay files under `diverged` with its "nothing below is trustworthy" banner instead of beside `approval-decided`: wardryx#57 |
| INS-2 on the 1.0 set (R1 GCP): the README one-liner of stack-single main `e078fcb` (wardryx, idryx, scopyx, trailryx-node at v1.0.0; tokenfuse v0.5.1) on two fresh `e2-standard-2` boxes: **109 s, exit 0, 22 ok, 0 fail** on each, 8 images pulled, 0 built; the same installer again on the first box with `WITH_EGRESS=1 WITH_RECORD=1`: **35 s, 22 ok**, the container set byte-identical, `scopyx:v1.0.0` and `trailryx-node:v1.0.0` pulled; `--profile egress` and `--profile record` started by hand: one `fetch_url` through scopyx 1.0.0's MCP door fetched the page and wardryx 1.0.0 wrote `policy_allow` with the recorded question; record-seal 1.0.0's first pass committed both cursors at byte 0 and said `nothing sealed here to pack yet`; `wardryx v1.0.0`, `idryx v1.0.0`, `trailryx-verify 1.0.0` from inside the containers | 2026-09-13 | GCP europe-west3-a, Ubuntu 26.04.1, Docker 29.1.3, boxes r1d-x86 and r1e-x86, both deleted within the quarter hour, `instances`/`disks`/`addresses` 0 afterwards | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-13-set-1.0/` `ins-2-run1-1.0-set.log`, `ins-2-run2-rerun-with-profiles.log`, `ins-2-run3-fresh-box.log`, `logs/r1e-install-1.log`, `scopyx-and-wardryx-journals.log`, `boxes.log` | under USD 0.05. The first box's full installer log was not copied off (scp of `/root` failed on permissions, read after the delete): the second box's is; no arm64 run on this set; the browser variant pulled by nobody |
| INS-5, REC-1, FAIL-4 on the 1.0 set (R2 GCP): stack-k8s main `b98c655` (wardryx, idryx, qryx, scopyx, trailryx-node at v1.0.0; tokenfuse v0.5.1; console v0.1.2) on one fresh five-node cluster from bare machines: `terraform apply` 63 s, `deploy-gcp.sh` **12 min 17 s** to **verify 15/0/1** and **security-tests 27/0/3**, exit 0, every image pulled; pods and CronJobs read back with exactly the pinned images; the three 1.0.0 routines fired by hand and complete within 15 s (`identity-sweep` 3 alerts on the tests' probe identity, `record-seal` 10 records under `taipanbox.dev` sealed, proof Full, pack **VERIFIED**, `crypto-trend` 0 sources with its 2 unreadable files named); FAIL-4: a freeze `PUT` through the admin API into the Postgres store, **deny before**, `verify.sh --freeze` restarted the plane, `the freeze survived a policy-plane restart`, **16/0/1**, **deny after** on a new `wardryx:v1.0.0` pod | 2026-09-13 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, driven from the Mac; `teardown.sh --yes` 15 destroyed, sweep `Nothing left`, direct query 0 instances/disks/addresses/forwarding rules/target pools/health checks, state 0 | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-13-set-1.0/` `logs/deploy-1.log`, `rec-1-routines-1.0-set.log`, `fail-4-freeze-1.0-set.log`, `teardown.log` | 0.33 h at USD 2.0387/h, about **USD 0.67**; whole go (~USD 3) spent under USD 0.75. `preflight.sh` rewrote `terraform.tfvars` to `c3d-highcpu-8` (quota 24 < 40) and was set back to c2d by hand (finding); scopyx on Kubernetes not applied (opt-in); the console pod has no curl, the freeze was written with python3 |
| terraform-provider-taipan **v0.2.0** published through the tag-triggered `release.yml` for the first time: goreleaser signed `_SHA256SUMS` with the rotated key `BBF9D1C7450D51D0` (the July key's private half was lost with the first Mac; the new public key registered in the namespace beside it), keyless Sigstore bundle, SPDX SBOM, provenance, 19 assets; the Registry serves `0.2.0`, and `terraform init` on `version = "0.2.0"` reports `self-signed, key ID BBF9D1C7450D51D0` | 2026-09-13 | GitHub Actions run 34782670860 (first attempt failed at `Import GPG key`, `Misformed armored text`, the secret's first paste; rerun of the failed job after the secret was updated), the public Registry, this Mac for `terraform init` | the Release page `v0.2.0`; `curl -s https://registry.terraform.io/v1/providers/TAIPANBOX/taipan/versions` | the secrets `GPG_PRIVATE_KEY` and `PASSPHRASE` were added by the owner in the GitHub UI; the tag was pushed by the owner (the auto-mode classifier refuses a tag push in this repository) |
| wardryx **v1.0.1** (#58, closes #57): `wardryx replay` files a hold on a spent approval token under `approval-spent`, not `diverged`; UPG-2 re-run on this Mac with the released `wardryx:v1.0.1` image in place of 1.0.0 (a 0.1.0 store on Postgres upgraded in place, the token presented twice): `approval-spent 1`, `diverged 0`, replay exit 0, 4 of 7 rows replayable | 2026-09-13 | this Mac, Docker Desktop, `postgres:16-alpine`, `ghcr.io/taipanbox/wardryx:v0.1.0` then `:v1.0.1`; `release.yml` run 34788167743 on the tag (image signed and attested, release page built on the first tag push since #56) | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/upg-2-wardryx-1.0.1.log`, `upg-2-run-1.0.1.sh` | the launchers keep pinning v1.0.0; the sibling gap (a deny on an invalid presented token replays as a divergence) is wardryx#59, not fixed |
| wardryx **v1.0.2** (#60, closes #59): `wardryx replay` files a deny on an approval token that did not verify under `approval-refused`, not `diverged`; on this Mac with the released `wardryx:v1.0.2` image on Postgres: a hold past the threshold, then the same action with a token nobody minted (`deny`, `presented approval_token is invalid (approval: token is malformed)`), replay over the file: `reproduced 1`, `approval-refused 1`, `diverged 0`, exit 0, 2 of 2 replayable | 2026-09-14 | this Mac, Docker Desktop, `postgres:16-alpine`, `ghcr.io/taipanbox/wardryx:v1.0.2`; `release.yml` run 34789009891 on the tag | `go-to-market-2026-09/evidence/1.0/r0-mac-2026-09-13/replay-refused-token-1.0.2.log`, `replay-refused-token-1.0.2.sh` | with 1.0.1's `approval-spent` and the original `approval-decided`, every token-dependent outcome the record cannot carry has a named row; the launchers keep pinning v1.0.0 |
| tokenfuse **v1.0.0** (#286, tag on `504359c`): the surface written and held (`compat/1.0.json`, 243 frozen names in 15 kinds; `scripts/compat-surface.sh` red first on a renamed route and "measured nothing" on a removed `where` file; seven teeth cases), the notes `docs/releases/v1.0.0.md` on the Release, `release.yml` green on every job: 8 binaries, 5 checksum files, the Sigstore bundle, SPDX and CycloneDX SBOMs, the provenance bundle (17 assets); four images amd64+arm64 (`tokenfuse`, `-cluster`, `tokenfuse-control-plane`, `tokenfuse-dashboard`); `docker run --rm ghcr.io/taipanbox/tokenfuse:v1.0.0 --version` prints `tokenfuse v1.0.0 (504359c)` (tokenfuse#275's stamp, also proven on `v0.5.1`: `tokenfuse v0.5.1 (2955236)`); estate-gates C19 `major 1`; the README moved to v1.0.0 in the commit after (#287, `bedf92b`) | 2026-09-14 | GitHub Actions run 34831579133; this Mac for the pull and `--version` | `gh release view v1.0.0 --repo TAIPANBOX/tokenfuse`; `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-14-set-1.1/` and `r2-…-set-1.1/` for the runs below | no behaviour change after v0.5.1; the compat gate is textual (a route in the router, not a route answering) |
| INS-2 on the 1.1 set (R1 GCP): stack-single `pin/tokenfuse-1.0.0` (tokenfuse and tokenfuse-control-plane v1.0.0, genaryx-console v1.0.0, stack-wg and stack-caddy v1.0.0; wardryx v1.0.2, idryx v1.0.0), the README one-liner fetched from the branch (`REPO_SINGLE_RAW`) on a fresh `e2-standard-2` box r1f-x86: **130 s, exit 0, 22 ok, 0 fail**, 8 images pulled, 0 built; again on the same box: **40 s, 22 ok**, the container set byte-identical; `tokenfuse v1.0.0 (504359c)` in the container; console 1.0.0 `/healthz` and `/` 200; stack-wg 1.0.0 `wg-op up on :51820`, stack-caddy 1.0.0 serving TLS on 443, the installer's five tunnel and TLS checks ok. **RUN-1 shape on the box**: 18 calls at 0.0006 USD through the stand's gateway (enforce, wardryx enforce and fail-closed) to the real Anthropic API, `claude-haiku-4-5-20251001`, `max_tokens 5`: **17 x 200 at 0.000033, call 18 `402 budget_exceeded`** with the wire body (`spent_usd 0.000561`), ledger 93.5 %, one `breaker_tripped` on the bus. Merged as stack-single#48 (`bd5ea31`), tagged **v1.1.0** with `docs/releases/v1.1.0.md` | 2026-09-14 | GCP europe-west3-a, Ubuntu 26.04.1, box deleted 10:27:53Z, direct query 0 instances/disks/addresses | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-14-set-1.1/` (`SUMMARY.md`, `ins-2-run1-1.1-set.log`, `ins-2-run2-and-run-1-1.1-set.log`, `run-1-to-the-402-1.1-set.log`, `console-ab-1.0.0-vs-0.1.2.log`, `logs/`) | under USD 0.02 plus USD 0.0006 of tokens. **Finding, pre-existing**: the console's `bus startup failed … Permission denied` at v1.0.0 and, swapped in on the same box, at v0.1.2 (genaryx#71). The cause first written here, a reader stopping at wardryx's 0700 archive directory, was wrong: the console creates its bus store under `TAIPAN_HOME`, a root-owned config mount here; measured on the image the same afternoon and fixed the same day (rows below). No arm64; the browser and record profiles pulled by nobody; no `budget_threshold` event at 80 % (the 2026-09-13 shape too) |
| INS-5, REC-1 on the 1.1 set (R2 GCP): stack-k8s `pin/tokenfuse-1.0.0` (tokenfuse v1.0.0 x3, genaryx-console v1.0.0 in the Deployment, the `quality-drift` CronJob and the four probe pods; wardryx v1.0.2, idryx, qryx, trailryx-node v1.0.0) on one fresh five-node cluster from bare machines: `preflight.sh` **kept the operator's `c2d-highcpu-8`** (stack-k8s#79 fixed by #80, `cba2fd2`, first live proof), `terraform apply` 64 s (15 resources, `hourly_usd 2.0387`), `deploy-gcp.sh` **11 min 30 s** to **verify 15/0/1** and **security-tests 27/0/3**, exit 0, every image pulled; every pod on the new pin, console 1.0.0 Ready with 0 restarts; `tokenfuse v1.0.0 (504359c)` in the pod. **RUN-1** from the console pod to the real Anthropic API: **17 x 200 at 0.000033, call 18 `402 budget_exceeded`**, ledger 93.5 %, one `breaker_tripped` with its `prev_hash` on the bus, the R1 box's numbers to the microdollar; **REC-1**: `record-seal` (trailryx-node 1.0.0) sealed the run's decisions and the gateway's event, **18 records in 3 segments, VERIFIED**; `quality-drift` on the 1.0.0 console: `skipped, missing store … (no verdryx eval has run here yet)`. Merged as stack-k8s#81 (`053d2a1`), tagged **v1.1.0** with `docs/releases/v1.1.0.md`; the tag builds `genaryx-console:v1.1.0`, `stack-caddy:v1.1.0` and `stack-wg:v1.1.0` (published, unproven) | 2026-09-14 | GCP `stack-k8s-gcp`, 5 x `c2d-highcpu-8` in europe-west3-a, k3s + Calico + Longhorn, driven from this Mac; `teardown.sh --yes` 15 destroyed, `Nothing left`, direct query 0 instances/disks/addresses/forwarding rules/target pools/health checks, state 0 | `go-to-market-2026-09/evidence/1.0/r2-gcp-2026-09-14-set-1.1/` (`SUMMARY.md`, `logs/`, `scenarios-1.1-set.log`, `run-1-to-the-402-1.1-set.log`, `rec-1-record-seal-1.1-set.log`, `console-bus-permission-denied.log`, `teardown.log`, the `r2-a..d.sh` drivers) | 0.31 h at USD 2.0387/h, about **USD 0.65**; the whole go (about USD 2.5) spent under USD 0.70. **Finding, pre-existing**: the console's `bus startup failed … Permission denied` on the cluster too (genaryx#71). The cause first written here, a reader stopping at `lost+found` and the policy archive, was wrong: the console creates its bus store under `TAIPAN_HOME`, the root-owned parent of a read-only ConfigMap mount; fixed the same day (rows below). Not run: scopyx on Kubernetes (opt-in), AWS, RUN-8, FAIL-4, UPG-2, the OpenAI door on the cluster |
| genaryx#71, the console's empty Bus Explorer, reproduced and closed: on this Mac the `genaryx-console:v1.0.0` image with the launchers' env (`HOME=/var/lib/stack`, `TAIPAN_HOME=/etc/genaryx/taipan`, uid 10001) and a clean events dir with no subdirectory: `environments/` mounted under a root-owned `/etc/genaryx/taipan` gives `bus startup failed … Permission denied`; the same with `/etc/genaryx/taipan` writable gives `bus LIVE` and `taipan/genaryx/single/console.sqlite`. So the cause was `durable_store_dir` = `<TAIPAN_HOME>/genaryx/<env>`, never the bus files. genaryx#72 (`ed72a7a`): candidates `GENARYX_STATE_DIR`, then `TAIPAN_HOME`, then `<HOME>/.taipan`, first creatable wins, a skipped one named on stderr, an uncreatable store naming every path; four tests red first (compile failure on the unfixed tree), two mutants caught by name; `features/` and `scripts/features-are-bound.sh` arrive in genaryx with three teeth cases; 784 tests. The `v1.1.1` image (built by the stack-k8s v1.1.1 tag) on this Mac: with `GENARYX_STATE_DIR` set, `bus LIVE` and the store there; without it, `durable store at /var/lib/stack/.taipan/genaryx/single (/etc/genaryx/taipan/genaryx/single could not be created: Permission denied)` then `bus LIVE` | 2026-09-14 | this Mac, Docker Desktop | `gh pr view 72 --repo TAIPANBOX/genaryx`; the reproduction commands are in the issue's correction comment | the three issues (genaryx#71, stack-single#49, stack-k8s#82) were retitled and corrected the same afternoon; the launchers' notes and the two rows above carried the wrong cause for about two hours |
| INS-3 on the 1.1.1 set (R1 GCP, arm64): stack-single `fix/console-state-dir-and-bus-live` (genaryx-console v1.1.1, `GENARYX_STATE_DIR` on the console, a 23rd installer check reading the console's startup line; every other pin as 1.1.0) by the README one-liner fetched from the branch on a fresh `t2a-standard-2` box r1g-arm64: **90 s, 22 ok, 1 FAIL** (`gateway exports agent events`, a one-shot log read that raced the log driver: the line stamped nine seconds before the check, ok on the next run, same container, `restarts=0`); again **25 s, 23 ok, 0 fail**, byte-identical; a third time with `check_log` (the two log-read checks retried up to ten seconds) **42 s, 23 ok**. Every image the `linux/arm64` manifest (postgres included): the arm64 gap of the 1.1 set closed. **The console at 1.1.1**: `bus LIVE on environment "single"` at startup, `console.sqlite` at `/var/lib/stack/genaryx/single/`; a budgeted run through the stand's gateway to the real Anthropic API (17 x 200 at 0.000033, call 18 `402 budget_exceeded`) and, within 10 s, the console's store holding wardryx's `policy_allow` x5 and the gateway's `breaker_tripped` for the run: gateway 1.0.0 to bus to console 1.1.1, end to end. Merged as stack-single#50 (`2f307a1`), tagged **v1.1.1**; the 1.1.0 notes corrected and re-published | 2026-09-14 | GCP europe-west4-a, Ubuntu 26.04.1 arm64, box deleted 12:01:11Z, direct query 0 instances/disks/addresses | `go-to-market-2026-09/evidence/1.0/r1-gcp-2026-09-14-arm64-set-1.1.1/` (`SUMMARY.md`, three run logs, `logs/`) | under USD 0.02 plus USD 0.0006 of tokens. Not proven: the Bus Explorer through a browser session (the store and the startup line were read); x86 on this exact set |
| stack-k8s **v1.1.1** (`7bff95b`, #83: `GENARYX_STATE_DIR` on the console Deployment, the 1.1.0 notes corrected; its tag built `genaryx-console:v1.1.1` from genaryx main with #72) and **v1.1.2** (`004a78b`, #84: the console pinned to v1.1.1 in the Deployment, `quality-drift` and the four probe pods; `verify.sh` reads the console's startup line, `bus LIVE` and never `bus startup failed`, an unreadable log and a moved line each a FAIL). **The v1.1.2 pin moved WITHOUT a cluster run, by decision on 2026-09-14** (the same image proven on the arm64 box by stack-single 1.1.1 the same day; the morning's 1.1.0 cluster run stands for the rest of the set) | 2026-09-14 | GitHub, the two tags and their Releases | `gh release view v1.1.2 --repo TAIPANBOX/stack-k8s` | the new `verify.sh` check has not run on a live cluster; `genaryx-console:v1.1.2`, `stack-caddy:v1.1.2`, `stack-wg:v1.1.2` built by the v1.1.2 tag are published and unproven, as every tag's composition is until the next run |
| tokenfuse **v1.0.1** (#288, tag on `6bd60ac`): three money-path corrections, each red first then green with ten mutants caught by name: an OpenAI cached token priced once (#267: 950/128 on `gpt-4o` settles 2215 micro-USD, not 2535), a one-hour cache write at its own rate (#282: the INT-2 call settles 281,716 micro-USD, what Claude Code computed, not 176,436; the published book gains `cache_write_1h_per_mtok_microusd`), a reachable provider's refusal on the bus (#260: one `dependency_failed` at stage `response` for 5xx/429/408/404/400, never a caller's 401/403/413/415/422). **Reviewed by a second model (Fable) before the push**, ten findings, four of them defects fixed in the same change (the netting lost on a later chunk, a `high` event on a caller's 401, a double record on a refused stream, heraldyx's false money sentence, closed in heraldyx#66 `b4ed257`; the SPEC word in agent-passport#65 `a6ed507`). `release.yml` run 34848113752 green on every job: 17 assets, four images amd64+arm64; `docker run --rm ghcr.io/taipanbox/tokenfuse:v1.0.1 --version` prints `tokenfuse v1.0.1 (6bd60ac)`, and `... constants` from the image publishes haiku 4.5's `cache_write_1h_per_mtok_microusd 2000000`. README to v1.0.1 in #289. Launchers re-pinned WITHOUT a run, by decision on 2026-09-14: stack-single **v1.1.2** (#51), stack-k8s **v1.1.3** (#85) | 2026-09-14 | GitHub Actions; this Mac for the pull, `--version` and `constants`; the tests and mutants on this Mac (1240 tests, teeth 56/56) | `gh release view v1.0.1 --repo TAIPANBOX/tokenfuse`; `docs/releases/v1.0.1.md`; the mutant list in tokenfuse CLAUDE.md invariants 45 to 47 | the 1-hour figure is asserted against Claude Code's own number, not re-measured against Anthropic's bill; the OpenAI multi-chunk shape is a reading of the parser; no rig ran the 1.0.1 image; heraldyx's sentence is on its main and not in a tag, so the pinned v0.2.2 still renders a refusal as unreachable |
| heraldyx **v0.2.3** (#67, tag on `f1ac1f7`): the sentence for a provider that answered and refused (#66, tokenfuse 1.0.1's `response` stage) behind a tag, and the first heraldyx tag through the signed release mechanics: `release.yml` creates `dist/` before the SBOMs and takes an existing tag on dispatch (wardryx#56's fix ported BEFORE the tag rather than met by it); run 34851604444 green on all three jobs, image `ghcr.io/taipanbox/heraldyx:v0.2.3` amd64+arm64 signed and attested, `sbom.spdx.json`, `sbom.cyclonedx.json`, `provenance.intoto.jsonl` on the Release with `docs/releases/v0.2.3.md`; README to v0.2.3 after the tag (#68). Launchers re-pinned WITHOUT a run, by decision on 2026-09-14: stack-single **v1.1.3** (#52), stack-k8s **v1.1.4** (#86) | 2026-09-14 | GitHub Actions; this Mac for `docker manifest inspect` | `gh release view v0.2.3 --repo TAIPANBOX/heraldyx` | no mail from a 1.0.1 gateway's refusal has been read; the sentence is held by its test |

## Sensors, proven

| What | When | On what | Artifact | Scope |
|---|---|---|---|---|
| Idryx's eBPF sensor (`idryx ebpf-capture`) attached and captured live traffic | 2026-09-08 | this Mac, Docker Desktop 29.7.2, LinuxKit kernel **7.0.12 aarch64**, BTF present (6 710 304 bytes), privileged container with `tracefs` mounted; binary cross-built `GOOS=linux GOARCH=arm64` | `evidence/captured.json` (21 flows) and `evidence/captured-pidhost.json` (2 flows) | The CO-RE program loads and attaches on arm64 and on a kernel it was never built against, which is the portability claim `connect.c` makes over tokenfuse's x86_64-only radar. It reported `proc:wget` and `proc:nc` connections correctly and counted 36 non-INET connects it deliberately skipped. **It has never run on a real fleet, a real cloud host, or against a real agent**; a 12-second capture in a container is not a deployment |

**What that run found: two defects, both reproduced, neither a named
limitation.** Checked before calling them that: SECURITY.md and README name no
container or PID-namespace caveat, and `identity.go` reasons about "fifty
containers each running the same binary", so a containerised sensor is inside
the intended envelope.

1. **The self-filter does nothing under a PID namespace.** `connect.c` reports
   `bpf_get_current_pid_tgid() >> 32`, the PID in the initial namespace;
   `capture_linux.go` compares it with `os.Getpid()`, the namespaced PID inside
   a container. Without `--pid=host` the sensor reported **16 of its own 21
   flows**; with `--pid=host`, which aligns the two and changes nothing else,
   2 flows and zero self-attributed (`evidence/captured.json` against
   `evidence/captured-pidhost.json`). The same PID is handed to
   `claimedAgentURI` for a `/proc` read, which SECURITY.md already calls worse
   than reading nothing.

2. **A Go program that merely resolves an LLM hostname is graded as having
   reached that API, HIGH.** When a name returns two or more addresses, Go's
   resolver sorts them per RFC 6724 and, to learn a source address for each
   candidate, `connect()`s a UDP socket to **every candidate on port 53**
   (`net/addrselect.go`, `srcAddrs`), sends nothing, closes. The tracepoint
   sees each one. `evidence/lookupprobe.go` (20 lines, only `net.LookupHost`)
   run under the sensor with `--pid=host` produced 8 flows to
   `generativelanguage.googleapis.com:53`, 2 to `api.openai.com:53` (exactly
   the A-record counts; anthropic returns one address, so no sort and no flow)
   and 6 to the resolver (`evidence/captured-probe.json`). Then
   `idryx detect -format json -source egress` on that file:
   **`unmanaged_egress` HIGH, "unattributed process reached an external LLM API
   directly (Google Gemini, OpenAI)"**, plus `shadow_ai` medium
   (`evidence/detect-captured-probe.json`). `matchLLM` strips the port before
   matching, so `:53` is graded exactly like `:443`.

   Finding 2 is why finding 1 looked the way it did: the sensor's own
   `resolveLLMHosts` is precisely such a Go lookup, so under a PID namespace the
   sensor files the HIGH finding against itself (`evidence/detect-captured.json`,
   `proc:idryx`, HIGH). Any Go agent, SDK or health check on a monitored host
   that resolves these names will draw the same HIGH.

The first write-up of this run, earlier the same day, explained the labels as
`destination()` mapping resolver traffic back to hostnames. **That was wrong**:
the labels are correct and the connects are real syscalls; the error is in
grading a port-53 connect as API egress. Corrected after the probe run, and left
here so the wrong explanation is not rediscovered.

Finding 2 is fixed: TAIPANBOX/idryx#68, squash-merged to `main` as `2717c4a` on 2026-09-08 after a green CI. Finding 1 is TAIPANBOX/idryx#66; its fix (TAIPANBOX/idryx#69, squash-merged to `main` as `19461f7` on 2026-09-08 after a green CI) hands the program the sensor's own PID namespace and decides self on `bpf_get_ns_current_pid_tgid()`. **Re-run of the exact #66 procedure on the fixed build, 2026-09-08, same Docker kernel, no `--pid=host`:** 3 flows, **0** the sensor's own (was 16 of 21), and a neighbour's `AGENT_PASSPORT_ID` read as `claimed:agent://acme.example/probe` (`evidence/fixed-nopidhost.json`; `fixed-pidhost.json` is identical in shape). Not exercised by that run: the initial-namespace branch of the `/proc` addressing, because Docker Desktop's "host" PID namespace is itself nested. Both are closed on `main` as of 2026-09-08. Fixing 1 needed a namespace-aware notion of self (`NSpid` from
`/proc/self/status`, or the sensor's own cgroup id, which every event already
carries). Fixing 2 is one condition: a connect on port 53 is name resolution,
not egress, whichever address it went to. tokenfuse's radar shares finding 2 by construction (`is_llm` flagged a provider
address on any port, and its BPF program takes UDP connects too); read off the code
on 2026-09-08, not run, since radar is x86_64-only. Fix: tokenfuse#263, squash-merged as `bbf48cf` on 2026-09-08 after the radar
job's re-run went green; the recorded red is a standalone rustc harness of the
same function, because ci.yml runs on pull_request only. The first CI run of
that PR failed on `System deps`: snapshot.ubuntu.com served 502/503 on every
dated snapshot for hours, and a re-run hung 57 minutes on a step that takes 79
seconds. bench.yml and release.yml depend on the same snapshot. tokenfuse#264 (squash-merged as
`b73dd10` on 2026-09-08; on that PR the probe ran for real in the radar job and in
both musl binary jobs, all green) makes the step fail in a minute with the reason
instead of hanging: a probe before each
snapshot-pinned apt line, bounded apt timeouts, the pin untouched; measured in
ubuntu:24.04 containers (outage 7 s, healthy 35 s) and held by three offline
teeth cases. **The minute held for "down", not for "flapping"**: in the second
outage the same evening InRelease answered, later index fetches 503ed, and apt's
per-fetch bounds (Retries=3 x 20 s over dozens of files) ran 27 minutes on the
#265 run (18:45:27 to 19:12:51) before Error-Mode=any ended the step. tokenfuse#266
puts `timeout-minutes: 6` on all three snapshot-pinned steps and drops Retries to 1;
the ceiling fired in GitHub's runner on its first run, tokenfuse PR #266 run
34269627993: `System deps` 19:33:19 to 19:39:31, 6 min 12 s, killed by the step
timeout while the service flapped (it was fully back by 19:36:44). 27 minutes to 6. Falling back to the live archive was then decided: `@yurii 2026-09-08`, "B закриваємо"
(no live-archive fallback; the pin stays), and the vendored-debs and digest-pinned-
container alternatives were judged "трохи зайве" for now. What was taken instead is
exposure reduction: the radar job runs only when crates/radar, ci.yml or the apt
probe changed (a `changes` job and a job-level `if`, so the required check reports
"skipped" rather than never reporting): tokenfuse#265, squash-merged as `30f0248`
on 2026-09-08. On its own PR the `changes` job printed "runs, radar, this workflow or
the apt probe changed"; the skip path is proven by replay only, the first unrelated
PR will show it live. The release-path ceiling also fired on #266's arm64 musl job:
`musl linker` 19:33:30 to 19:39:42, 6 min 12 s, and again on its re-run at
19:48:44 to 19:54:57 while the service was up but slow (10 to 40 s per arm64
index file, sixty-odd files). That is the intended shape now: a degraded snapshot
costs six minutes and a clear message, and the job is re-run when the service
is fast again. #266 did exactly that: re-run at 20:02 once the service served arm64
indexes quickly, green, squash-merged as `3576880` on 2026-09-08. musl was then measured the same day without a
dual-stack network (`/etc/hosts` with one IPv4 and one IPv6 entry; the tracepoint
fires before the syscall, so an unroutable IPv6 probe is still captured):
busybox `wget` on Alpine `connect()`s **each candidate on port 65535** before
the real attempt, glibc (`getent`, `curl`) shows no probe at all
(`evidence/resolvers.json`). So #68's port-53 rule misses musl; the fix is the
443-only rule radar already has: TAIPANBOX/idryx#71 (`destination()` consults
the provider map only on 443; red-first on 65535, 8080 and 80), squash-merged as
`4677225` on 2026-09-08 after a green CI; #70 closed with it.

### 2026-09-09: the sensor moved off the syscall boundary, and both reasons were measured first

Same machine and kernel as the run above (this Mac, Docker Desktop, LinuxKit
**7.0.12 aarch64**, 10 CPUs, privileged container with `tracefs` mounted,
binaries cross-built `GOOS=linux GOARCH=arm64`). Both defects were measured
against the sensor as it stood on `main` at `4677225`, then against the branch,
with the same workload under each. Probes and captures in
`evidence/2026-09-09-*`.

**1. A second thread could choose what the sensor recorded.** The
`sys_enter_connect` tracepoint fires at the syscall boundary, where the
destination is still a pointer into the calling process's memory that the kernel
has not copied yet. `evidence/2026-09-09-toctou-race.c` calls `connect()` in a
loop from a shared sockaddr while another thread flips the port between one with
a listener and one without, so `connect()`'s own return says which port the
kernel actually used. Three runs:

| connections | old sensor | the branch |
|---|---|---|
| 20,000 | 58 recorded a port the kernel did not use | exact |
| 30,000 | 126 | exact |
| 25,000 | 221 | exact |

**2. io_uring was invisible, silently and completely.** `IORING_OP_CONNECT`
copies its address at submission and calls `__sys_connect_file` directly, never
reaching the tracepoint. One io_uring connection and one ordinary one, to
`127.0.0.1:11434` so nothing left the machine
(`evidence/2026-09-09-iouring-connect.c`): the old sensor captured **1** flow
(`2026-09-09-iouring-old.json`), the branch captures **2**
(`2026-09-09-iouring-new.json`), the io_uring one arriving as
`proc:iouring_connect` with the submitting program's own name.

**The fix these two runs measured was then REFUSED, and that is the part worth
reading.** `fentry` on `inet_stream_connect` and `inet_dgram_connect` closed
both defects and was built as TAIPANBOX/idryx#74. A review priced it and it was
closed unmerged, because attaching below the syscall costs more than it buys
today:

- `security_socket_connect` runs BEFORE `sock->ops->connect` in
  `__sys_connect_file` (read at net/socket.c, v6.12 and v7.0), so hooking the
  protocol's connect loses every attempt an LSM denied. For an identity plane,
  the denied attempt is the event most worth having.
- `fentry` on a kernel function needs a BPF trampoline, and arm64 gains one at
  6.4: on 6.1 `bpf_arch_text_poke` pokes only BPF text, `register_fentry`
  returns `-ENOTSUPP` without `tr->fops`, and `DYNAMIC_FTRACE_WITH_DIRECT_CALLS`
  enters arm64's Kconfig at 6.4 (all four read at those tags). The sensor's
  floor would move from 5.8 to 6.4 on arm64, taking the portability that is its
  one advantage over tokenfuse's radar.
- SCTP has its own `sctp_inet_connect`, MPTCP on 6.1 to 6.3 bypasses both
  hooked functions, and one io_uring TCP connect can produce two records because
  `io_connect` re-issues after `EINPROGRESS`. The "2 flows" above was 1 plus 1
  only because nothing was listening on the target port.

**So the two defects are now named limitations rather than silences**
(TAIPANBOX/idryx#75, SECURITY.md), and the redesign is recorded in that
repository's AGENTS.md with what to build (one program on `__sys_connect_file`,
which precedes the LSM hook, is reached by both paths and carries `addrlen`),
the two decisions to take first, and the test matrix. `@decided 2026-09-09`:
redesign once, deliberately, when the sensor goes into a real deployment.

**Wrong once, and worth recording.** The first race probe wrote both port values
to the same field in adjacent statements, `-O2` deleted the first as dead, the
buffer held one value for the whole run and both sensors agreed with ground
truth. A probe that quietly stops racing reports two sensors as identical.

**The committed BPF object did not reproduce from its own source.** Separately,
and found while reading the same code: `internal/ebpfcapture/bpf_bpfel.o` and
`bpf_bpfeb.o` are compiled binaries in git that `//go:embed` puts into every
released binary and published image, and nothing compared them with the C beside
them. CI regenerated `vmlinux.h` from the runner's kernel and ran `go generate`
over the top, overwriting the object before anything looked; the step was even
named "catches connect.c/committed-output drift". Rebuilt in an `ubuntu:24.04`
container with the toolchain that job installs (clang 18.1.3, libbpf 1.3.0, Go
1.27.0) against the committed header, the object committed on 2026-09-08 came
out two instructions shorter, both a redundant `r9 = 0x0` a newer clang proves
unnecessary. Benign, and indistinguishable at the time from a substituted
program. TAIPANBOX/idryx#73 adds `scripts/object-matches-its-source.sh` with a
pinned compiler and four cases in that repository's teeth harness.

**Architecture is not a variable in that comparison**, measured twice: one clang
gave identical bytes on aarch64 and x86_64 containers, and the object generated
here on aarch64 then matched byte for byte on GitHub's own x86_64 runner in the
`ebpf` job of #74.

**What none of this proves.** One kernel, in a container, on one Mac. Never a
fleet, never a cloud host, never x86_64 for the sensor itself, and never against
a real agent. It is not a claim of unevadability either: a host compromised
enough to load kernel code, or to reach the network by a path that never calls
`sock->ops->connect`, is outside all of it. And a kernel that refuses to attach
`fentry` to those two symbols now fails the sensor loudly rather than capturing
part of the traffic; no such kernel has been tried.

## Deployment, NOT proven, with the scope of the gap

| What | Why it is open | Last touched |
|---|---|---|
| `stack-up` on published images | Not migrated. It builds from source by design and says so: it is the local sandbox and needs Rust, Go, Node and Python on the host. `stack-single` migrated on 2026-09-01 (stack-single#27 and #28) and now compiles nothing by default | 2026-09-01 |
| arm64 running on arm64, as a cluster | `stack-single` ran on a GCP `t2a-standard-2` on 2026-09-13 (INS-3 above, every image linux/arm64); no CLUSTER has been raised on Graviton or any other arm64 host | 2026-09-13 |
| Windows, anywhere | Nothing, ever | n/a |
| The recipes' CI matrix (INT-1's second half) | The offline stub refuses to start on the OpenAI wire by design, so a CI job needs a real OpenAI-shaped upstream; tokenfuse #276 ships the recipes without it | 2026-09-13 |
| R1: RUN-7, POL-6, REC-4 | RUN-7 needs vouchryx, which stack-single does not carry (a stack-up build on the 2 vCPU box was not attempted); POL-6's eBPF radar is not a released asset; REC-4 needs SMTP to a mailbox of his | 2026-09-13 |
| R2 GCP: UPG-2, UPG-4, REC-5, REC-6, the console halves of POL-2 and REC-1, the `/v1/chat/completions` half of RUN-1, raft re-election in FAIL-3, `alert_sent` in FAIL-1 | UPG-2 and UPG-4 have no 1.0 image to upgrade to yet; REC-5's D6 task set was not located and is real model spend; REC-6 and the passkey ceremonies are his; the pinned gateway is v0.4.4 (door is v0.5.0); HA is compiled out; no SMTP server | 2026-09-13 |
| R2 AWS: INS-6, POL-1, POL-2, POL-4, REC-2, INT-4, FAIL-1, FAIL-3 | Measured on GCP the same day and not repeated on AWS, a cost choice, not an inability | 2026-09-13 |
| R3: RUN-2 (the wrong door for the wire), REC-5 (the D6 set), a real OpenAI key, Nova on Bedrock's OpenAI door, the #283 fix on a running image | RUN-2 and REC-5 were not run on R3; no OpenAI key exists (OpenRouter stood in); Bedrock does not serve Nova as OpenAI wire on this account; #283 is fixed on main (#284) and measured by its tests, not yet by a released image behind a real provider | 2026-09-13 |

## Traps that bite an operator and that no gate holds

| Trap | Where | Status |
|---|---|---|
| `apply -k` restores `TRAILRYX_TRUST_DOMAIN: set-me.invalid`, and the record plane then refuses every event, which reads as a quiet night | GOTCHAS 90 | Named limitation, not a defect: `cloud/{aws,gcp}/deploy-*.sh` carry `--trust-domain` and their own comment calls the empty default "the loud state and the right default". The base `deploy.sh` has no such flag, and **no gate refuses a finished deploy with the placeholder still set** |
| A delete takes the volume and leaves the secret; `60-harden-neighbours.yaml` cannot be removed at all | GOTCHAS 91 | Measured and named. GOTCHAS 91 says outright "Nothing below is a bug"; three of four are Kubernetes as documented, and only `48` taking the plain scopyx with it is ours |
| A GCP load balancer with a healthy backend that carries nothing | GOTCHAS 69 | Open, and marked Platform: every GCP-side object verified correct |

## The estate's public surface

Do not recall any of it. Run:

```
./scripts/outside-view.py
```

Two findings from its first run, 2026-09-01, that had been guessed wrong before it existed:

- **Every image pinned by a manifest is anonymously pullable.** An earlier claim
  that genaryx had no public image came from asking the registry for `genaryx`;
  the manifests pin `genaryx-console`. The script now derives names from the
  manifests, so that class of error cannot recur.
- **`verdryx` and `vouchryx` are pinned by no manifest at all.** That is a
  different statement from "their image is missing", and the correct one: they
  are not deployed as containers anywhere, which is consistent with running
  inside the console image.

And one gap the script exposes rather than fixes: **21 of 22 repositories
declare no `distribution`**, so where a human installs them has no declared
answer anywhere in the estate. Only Engram declares one (`engdbram`).
